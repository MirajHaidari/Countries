Province:
pg_dump -U postgres -d rmis_db -t province --data-only --column-inserts --encoding=UTF8 -f province.sql

Districts:
pg_dump -U postgres -d rmis_db -t district --data-only --column-inserts --encoding=UTF8 -f
districts.sql

.TXT format in PgAdmin4:

COPY province TO 'D:\tt\district.txt' WITH (
    FORMAT TEXT,
    ENCODING 'UTF8'
);